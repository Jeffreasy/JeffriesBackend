Based on my analysis of the verified findings, here is the consolidated action plan. Note the deduplication: findings #2 and #16 are the same concurrency/identity_key issue, and #3, #26, and the LaventeCare-delete finding all cluster around the prune/mirror data-loss theme. The verifiers' severity corrections (several highs → medium/low) are applied.

## Verified issues

Ranked by real-world severity. Confirmed data-loss/security first, then correctness, then UX/consistency.

| # | Sev | File:line | Issue | Impact | Fix |
|---|-----|-----------|-------|--------|-----|
| 1 | **HIGH** | `store/contacts.go:815` | `collapseDuplicateContacts` re-points only 5 child tables + labels, then deletes the dup — but `whatsapp_conversations`/`whatsapp_summaries` (ON DELETE CASCADE) are not re-pointed | Collapsing a dup person **permanently deletes** all imported WhatsApp chats, messages, and the AI-facing summaries attached to the loser row, with no trace | Add `whatsapp_conversations` and `whatsapp_summaries` to the re-point list before the DELETE (messages follow their conversation FK) |
| 2 | **HIGH** | `store/contacts.go:757` | Prune step hard-deletes every `source='laventecare'` person with no remaining LC org link, cascading away all user enrichment | Deleting a person's **last** company in LaventeCare irrecoverably wipes their VIP labels, birthdays, facts, interactions, WhatsApp data — when intent was only to drop a company affiliation | Don't hard-delete pruned people that carry enrichment: archive (`archived=true`, clear org links) or flip `source='manual'` when labels/dates/facts/interactions/whatsapp rows exist; delete only truly-empty mirror rows |
| 3 | **HIGH** | `app/contacten/page.tsx:1242` (verifier: medium) | LabelsEditor Enter handler: `exactExists` includes assigned labels but `suggestions` (substring, excludes assigned) can have a *different* label at `[0]` | Typing an already-assigned/exact name + Enter **silently assigns the wrong label** (e.g. type "werk" → assigns "Netwerk"), no error | On Enter resolve the exact case-insensitive catalog match explicitly; assign it if unassigned, no-op if assigned, only `createAndAssign` when no exact match. Never fall back to `suggestions[0]` |
| 4 | **MED** | `store/contact_channels.go:199` | `DeleteInteraction` unconditionally recomputes `last_contacted_at = MAX(occurred_at)` over remaining interactions, ignoring non-interaction sources (`TouchLastContact`, WhatsApp import) | Deleting an interaction can **null out a manual "just contacted" touch** → contact wrongly resurfaces in StaleContacts/reconnect-nudge as never-contacted | Only lower `last_contacted_at` when the deleted row *was* the max, else leave untouched; or `GREATEST` against the existing value. Mirrors `AddInteraction:180` which already uses GREATEST |
| 5 | **MED** | `store/contact_channels.go:180` | `AddInteraction` sets `last_contacted_at` with no upper bound; handler accepts any RFC3339 incl. future | A future `occurred_at` pushes `last_contacted_at` into the future → StaleContacts SQL filter (`< now()-interval`) never matches → **reconnect nudge suppressed** until the date passes (effectively permanent for a wrong-year typo) | Reject or clamp future `occurred_at` to `now()` in handler/`AddInteraction` |
| 6 | **MED** | `store/contacts.go:216` | `LabelMatchAll` binds `cntPh = len(names)` but `loweredNonEmpty` doesn't dedup; `COUNT(DISTINCT lower(name))` can't equal a count inflated by duplicates | Filtering with a duplicated label (`['VIP','vip']`) + `match='all'` **returns zero rows** though matches exist (AI tool + UI pass labels through with no dedup) | De-duplicate lowered names in `loweredNonEmpty` (or before computing `cntPh`) so the required count equals the distinct-label count |
| 7 | **MED** | `app/contacten/page.tsx:1746` | WhatsApp import never sends `is_group`; group exports stored `is_group=false`; summary lists all participants but is attached to a single 1:1 contact and fed to the AI | Importing a family group chat onto "Mama" stores it as 1:1; the AI reasons about Mama using **group-wide metadata** | Auto-detect groups (>2 distinct participants) or add an `is_group` toggle; make `buildWhatsAppSummary`/AI path group-aware (setting the flag alone is insufficient — the summary ignores it) |
| 8 | **MED** | `app/contacten/page.tsx:59` (delete/merge filter) | Deleting/merging a label that is the active `labelFilter` leaves its stale ID in state; predicate then matches zero contacts with no visible chip | List shows "Geen resultaten" with no active filter shown and, if it was the only label, no "wis labels" button — user stuck without a reload | After labels refetch, prune `labelFilter` to IDs still present in `labels` (derived effect); same on merge |
| 9 | **MED** | `app/contacten/page.tsx:63` | Bulk-select `selected` Set is never pruned when filter/search/sort narrows the visible set | BulkBar count and `applyBulkLabel` operate on **now-hidden contacts** the user didn't reconfirm (labels all 10 while 2 visible) | In the existing `windowSig` block, intersect `selected` with current `filtered` ids (or clear selection) before rendering BulkBar/applyBulkLabel |
| 10 | **MED** | `store/contacts.go:704` (= finding #16, dedup) | No unique index on `(user_id, identity_key)`; non-atomic SELECT-then-INSERT under READ COMMITTED with no advisory lock; concurrent write-through + RunOnStart cron race | Two concurrent syncs both insert the same person → transient **duplicate contacts** shown until the next reconcile collapses one (which can lose enrichment on the loser per #1) | Partial `CREATE UNIQUE INDEX ... ON contacts(user_id, identity_key) WHERE source='laventecare'` + `INSERT ... ON CONFLICT DO UPDATE RETURNING id`; or `pg_advisory_xact_lock(hashtextextended(userID,...))` mirroring `laventecare.go:4093` |
| 11 | **MED** | `engine/pending_actions.go:356` (verifier: medium) | `contactBijwerken` confirmation shows only the raw `contact_id` UUID (+ optional display_name); changed field/value never surfaced | Approver on Telegram/Settings sees an **opaque UUID** with no name and no indication of what's being overwritten → can't catch a wrong-contact or hallucinated value | Add `contactBijwerken`/`contactFeitOnthouden`/`contactLabelToevoegen` branches to the enrichment switch that resolve `contact_id → display_name` via `contactStore.Get`, and include the changed field/value |
| 12 | **MED** | `engine/pending_actions.go:358,360` | `contactFeitOnthouden` and `contactLabelToevoegen` confirmations show contact only as a raw UUID | Approver can't tell **who** a sensitive fact/label attaches to → approves attaching e.g. "is gescheiden" to the wrong Jan | Enrich with resolved `display_name` via `contactStore.Get` (same switch as #11) |
| 13 | **MED** | `engine/cron_workers.go:657` (verifier: low) | Reconnect-nudge/reminder cooldown consumed by `shouldFireAlert` *before* confirming a non-empty candidate list | On an empty-candidate tick the 6d/20h cooldown is burned; a same-window data change can suppress the legit nudge (bounded to the morning window, not a full week) | Move `shouldFireAlert` to just before `SendProactiveNotification`, after the list is confirmed non-empty |
| 14 | **LOW** | `store/contact_labels.go:229` | `AssignLabelByName` calls `CreateLabel` before `AssignLabel`'s `assertOwns`; a valid-but-nonexistent `contact_id` mints the label then fails | Orphan zero-count label **pollutes** `labelsOpvragen`/label manager permanently on an AI misfire | Call `assertOwns(contactID)` before `CreateLabel`, or wrap create+assign in one tx that rolls back on assignment failure |
| 15 | **LOW** | `handler/contact_channels.go:67` | Nested child routes (`/{id}/channels/{channelID}`, dates, facts, interactions, whatsapp) ignore parent `{id}`; store scopes by `(user_id, child_id)` only | Child of contact A is deletable/readable via a URL naming contact B — REST-contract/integrity defect (no cross-user breach; single-tenant). **Note: `RemoveLabel` is NOT affected** (it does validate `{id}`) | Add `AND contact_id=$3` to the affected store queries (return ErrNoRows on mismatch), or drop the redundant `{id}` segment |
| 16 | **LOW** | `handler/contacts.go:482` | `WhatsAppMessages` ignores `{id}`; `ListWhatsAppMessages` filters only `(user_id, conversation_id)` | Any conversation's raw messages retrievable under any contact path (same-user only) | Add `EXISTS(... whatsapp_conversations WHERE id=convID AND user_id=user AND contact_id=contactID)` and pass `{id}` into the handler |
| 17 | **LOW** | `store/contact_labels.go:219` | `RemoveLabel` ignores `RowsAffected`; handler always returns 200 `"removed"` | Removing a wrong/nonexistent assignment reports success (no-op reported as done) | Check `tag.RowsAffected()==0 → ErrNoRows` → 404, mirroring sibling label endpoints |
| 18 | **LOW** | `store/contacts.go:298` | `Create` inserts email/phone/notes/address raw (no trim, no NULLIF); `Update.addText` trims + NULLIFs | `contactMaken` with `email:''`/`phone:'  '` persists empty/whitespace non-NULL strings; `phone IS NULL` queries then diverge between create/update paths | Trim + NULLIF in `Create` matching `addText`, or normalize `*string` args in the `contactMaken` executor case |
| 19 | **LOW** | `engine/pending_actions.go:354` | `contactMaken` confirmation shows only the name; email/phone/relationship_types and auto-created labels hidden | Approving silently **creates new labels** in the vocabulary the approver never saw | Append relationship_types/labels (+email/phone) to the summary (array-join helper, not `value()`) |
| 20 | **LOW** | `engine/engine.go:124` | Alert cooldown state (`alertFiredAt`) is in-memory only, wiped on every deploy/restart | A deploy inside the morning window → **duplicate** "Belangrijke datums" Telegram messages (briefing cron avoids this via `briefing_sent` claim table) | Back reminder/nudge dedup with a persistent per-day/per-window claim row like `briefing_sent` |
| 21 | **LOW** | `store/laventecare.go:432` | Write-through sync failures only `slog.Warn`'d; no dirty-flag/outbox; mirror stale up to 6h | Deleted person still shown in module + AI context for up to 6h on a transient sync failure (self-heals; source-of-truth LC tables stay correct) | Set a `contacts_mirror_dirty` marker (transactionally) so a short-interval worker reconciles promptly |
| 22 | **LOW** | `app/contacten/page.tsx:1553` | Rename input is uncontrolled; on 409 collision the rejected text stays in the DOM | Row shows rejected name while DB/chips show the real one until modal reopens | Reset `e.target.value = l.name` in the catch, or make the field controlled |
| 23 | **LOW** | `app/contacten/page.tsx:1181` | `assignExisting`/`createAndAssign` clear query but never `setOpen(false)` | After picking a label the full suggestion list re-opens — looks like a new menu / failed action | Call `setOpen(false)` (or blur input) after a successful assign/create |
| 24 | **LOW** | `app/contacten/page.tsx:1520` | `PalettePicker` is `position:absolute` inside the modal's `overflow-y-auto` scroll container | For labels near the list bottom, lower color swatches are **clipped and unreachable** | Render palette in a portal/fixed layer, or flip above the row when near the bottom |
| 25 | **LOW** | `app/contacten/page.tsx:908` | Detail modal renders primary email/phone and channels separately with no dedup | Same value entered as both primary field and a channel appears twice, looking like duplicate data | Dedup channels whose value equals the primary email/phone before rendering, or unify primaries into the channels list |
| 26 | **LOW** | `app/contacten/page.tsx:1069` | UI hides Delete for `source='laventecare'` but backend `store.Delete` has no source guard | A script/manual `DELETE /contacts/{id}` on an LC row wipes local enrichment; sync resurrects a bare copy (partial silent data-loss). **No AI delete tool exists** | Enforce in `store.Delete`/handler: refuse/409 when `source='laventecare'`, mirroring the UI guard |
| 27 | **LOW** | `app/contacten/page.tsx:154` | Group-by-label renders the full (up-to-500) list unwindowed; multi-label contacts render N cards | Toggling "Groepeer op label" can mount 1000+ cards → jank (gated behind explicit toggle) | Cap/windowize grouped output (per-group "toon meer" or virtualize) |
| 28 | **LOW** | `app/contacten/page.tsx:384` | Inline `onVisible` arrow → new identity each render → observer effect (`[onVisible]`) tears down/recreates; re-observe re-fires on the still-intersecting sentinel | Possible over-pagination (visibleCount jumps by multiples of 60) during mutation-driven re-render bursts (self-limiting) | `useCallback` the `onVisible` identity; unobserve after firing until count grows |
| 29 | **LOW** | `store/contacts.go:623` | Important-date validation allows day 1-31 for any month; `clampDate` silently clamps at compute time | User fat-fingers "31 apr"; it saves, UI prints "31 apr", reminder fires 30 Apr — nonsense date never flagged | Validate day against the specific month (reject 31 for 30-day months, 30/31 for Feb), or normalize+echo the clamped date |
| 30 | **LOW** | `app/contacten/page.tsx:84` | Client-side search matches label names but only within the ≤500-row loaded window; backend `?q=` matches name/email/notes (not labels) and is never called | Label-name search silently incomplete past 500 contacts; search semantics diverge from backend (labels have a dedicated facet, so the API-consumer angle is weak) | Fold label matching into backend `List` q (join assignments) for parity, or document search scope + move search server-side |
| 31 | **LOW** | `app/contacten/page.tsx:77` | Multi-label UI filter is hard-coded match-ANY (client-side); no AND/OR toggle | Selecting two labels unions instead of intersects — a defensible default but a surprise for some (see also gap #6) | Add an AND/OR toggle switching the client filter between `.some()`/`.every()` |

**De-duplicated:** Findings #2 and #16 in the input JSON are the same concurrency defect (rendered once as issue #10). The three LaventeCare data-loss findings (WhatsApp cascade, last-company prune, API-delete resurrection) are distinct mechanisms and kept separate (issues #1, #2, #26). Findings #11/#12/#19 share one root cause (missing pending-action name enrichment) — fix them together in the same switch.

**Suggested fix ordering:** Ship #1 + #2 first (same file, `collapseDuplicateContacts`/prune — irreversible data-loss). #3 is a fast frontend one-liner with real wrong-write impact. Then #4–#9 (correctness), then the pending-action enrichment cluster (#11/#12/#19 in one pass), then the low-severity backlog.

## Enterprise coverage gaps

The `kind:"gap"` items, ranked by value. Manual-merge is called out first as the user's explicit top request.

| Rank | Sev | Gap | Why it matters / user impact | Concrete build |
|------|-----|-----|------------------------------|----------------|
| **1** ⭐ | **HIGH** (verifier: medium) | **Manual merge of two arbitrary contacts — does not exist** (`store/contacts.go:786`). The only fold logic is the private, LaventeCare-only `collapseDuplicateContacts` (`WHERE source='laventecare' AND identity_key`). No store method, route, `api.ts` fn, or UI. `GitMerge` in the UI is wired to **label** merge only. | **This is the user's #1 ask.** A person entered twice (typed manually, or manual + WhatsApp/LC import with a slightly different name so identity_keys differ) can **never** be unified — labels, dates, facts, channels, interactions, org links stay split forever, and birthday/reconnect crons double-count them. | Add public `MergeContacts(ctx, userID, fromID, toID)` re-pointing all child tables + WhatsApp rows in one tx (`ON CONFLICT DO NOTHING` for the label composite PK), converging empty core fields onto the target, deleting the source. Then `POST /contacts/{id}/merge {into}` + `contactenApi.contactMerge` + a picker in the detail modal. **Guard:** prefer the LaventeCare row as survivor (or block cross-source merges) so the next mirror sync doesn't clobber it. |
| 2 | MED | **No un-merge / split** (`store/contacts.go:786`). Merge/collapse is one-way and lossy — no provenance of which rows came from which original. An over-eager `identity_key` collision (two email-less people sharing a name → same key) **silently, permanently merges distinct people** on the next sync. | A wrong auto-collapse or (future) manual merge is irreversible; recovery = delete and rebuild both, losing all local enrichment. | Record provenance (`merged_from` column or `contact_merge_log` capturing source id + moved child-row ids) to enable a split; **and/or** require email (or another disambiguator) in `identity_key` so nameless namesakes aren't auto-collapsed, plus a "these are different people" escape hatch. |
| 3 | HIGH (verifier: medium) | **Cannot edit org affiliations for MANUAL contacts** (`handler/contacts.go:94`). The multi-org `contact_organizations` table is populated **only** by the LC mirror; no `Add/Update/RemoveOrganization` store method, no `/contacts/{id}/organizations` route, no org field in the form. | A manual friend can never be linked to a company — the entire multi-org model the module is built around is **LaventeCare-only**. | Add `Add/Update/RemoveOrganization` store methods (`source='manual'`, `lc_contact_id NULL`) + `POST/PATCH/DELETE /contacts/{id}/organizations` routes + an org+role picker (companies from `laventecareApi`) in the form/detail modal; skip `source='laventecare'` links from manual edit so the sync stays authoritative. |
| 4 | MED | **Archive is backend-only — no UI** (`app/contacten/page.tsx:1068`). Model/API/AI-tool fully support `archived`, but the frontend has zero archive references; detail footer offers only Delete + Edit, list never requests `includeArchived`, no restore. | Only way to hide a contact is the **hard cascading Delete** (destroys dates/facts/interactions). Rows archived via the AI tool become invisible with no way to view/restore them. | Add "Archiveren"/"Herstellen" action calling `update {archived}`, a "toon gearchiveerd" toggle passing `includeArchived=true`, and a visual marker for archived cards. |
| 5 | MED | **No duplicate detection for manual contacts** (`store/contacts.go:288`). `Create` does zero dedup; no unique index on `(user_id, lower(email))` or name for manual rows; no warn-on-similar. | Same person entered twice persists silently; combined with the missing manual-merge (gap #1) → **permanent unresolvable duplicates** with double reminders. | On `Create`, look up existing manual contacts by `lower(email)`/`lower(display_name)` → 409 or a "possible duplicate — toch aanmaken / open bestaande" payload; optionally a periodic duplicate-suggestions view feeding the merge flow. |
| 6 | MED (verifier: low) | **Label AND-filter (`LabelMatchAll`) unreachable from UI** (`app/contacten/page.tsx:77`). Store implements the full AND filter but no HTTP/api.ts/UI path sets it; UI filters client-side match-ANY only. | Selecting "VIP" + "kerstkaart" unions instead of intersects, with no toggle. (Purely a client-side UI gap — filtering is local over the loaded set, so no backend plumbing is strictly required.) | Add an "alle labels"/"een van de labels" toggle switching the client filter between `.every()`/`.some()` (backend `LabelMatchAll` optional). |
| 7 | MED | **Channels can't be edited or promoted to primary** (`handler/contact_channels.go:66`). Backend supports `is_primary` (demotes siblings) but there's no Update/promote endpoint and the form never sends `is_primary`. | Fixing a typo requires delete + re-add; can't mark which of several emails is primary — the sibling-demotion + primary-ordering logic is unreachable from the UI. | Add `PATCH /contacts/{id}/channels/{channelID}` (edit value/label/kind, set `is_primary` demoting same-kind siblings) + edit + "maak primair" affordance per channel. |
| 8 | LOW | **No CSV / vCard export** (`server/routes.go:262`). No export endpoint or UI anywhere. | User can't back up or import their address book into a phone/Google Contacts (only raw JSON via the List API). | `GET /contacts/export?format=csv|vcard` streaming names, relationship types, primary+channel emails/phones, org affiliations, important dates + an "Exporteer" header button. |
| 9 | LOW | **No true server-side pagination** (`store/contacts.go:197`). `List` has only a hard LIMIT (no OFFSET/cursor/total); frontend fetches a fixed **500** and windows client-side, eagerly hydrating labels + orgs for the whole set. | Doesn't scale; at >500 contacts the extras are **silently truncated** with no page to reach them (see also issue "500-cap" below). | Add offset/cursor + total/next-cursor to the List response, keep search/filter server-side, fetch pages as the sentinel scrolls. |
| 10 | MED | **List hard-capped at 500 with no pagination or indicator** (`hooks/useContacten.ts:22`). (Listed as a gap; it's the acute symptom of gap #9.) Backend `ORDER BY display_name LIMIT 500` keeps the alphabetically-first 500. | At 600 contacts, everyone sorting after the 500th is **invisible everywhere** — header count, search, filters, groups, AI — with no error or hint. | Real pagination/streamed fetch, or at minimum a "meer dan 500 contacten — verfijn je zoekopdracht" banner + push search to the backend `?q=`. |
| 11 | LOW | **No audit trail / change history** (`store/contacts.go:319`). Mutations overwrite in place; delete cascades; mirror sync silently overwrites/prunes. No `contact_audit` table. (An unused `audit_logs` table exists but has no writers and no old/new columns.) | For a years-long CRM there's no way to see when a phone/email changed, its old value, or that the sync overwrote a field — no recovery. | Lightweight `contact_audit` (contact_id, field, old, new, actor: manual\|ai\|laventecare-sync, at) written from Update/Delete/mirror-sync; optional "wijzigingen" timeline in the detail modal. |
| 12 | LOW | **No contact photos / avatars** (`model/contacts.go:14`). No avatar/photo column, upload path, or render slot; UI always shows a single-letter initial tile. | Several "M" contacts (Mama/Mark/Marieke) are visually indistinguishable at a glance. | Add `avatar_url` (or blob/object-store ref) column + upload/set endpoint + render in ContactCard/detail with the initial as fallback. |

**Note on gaps #9 and #10:** they are the same root problem (no server pagination) — the 500-cap is the user-visible symptom of the missing pagination primitive. Build them as one workstream, and it also resolves the search-completeness half of issue #30.

Files are all under `C:\Users\jeffrey\Desktop\Projecten\JeffriesBackend\backend\internal\` (backend) and the `JeffriesHomeapp` repo (`app/contacten/page.tsx`, `hooks/useContacten.ts`, `lib/api.ts`).